# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['securekeygen.py'],
    pathex=['C:\\Users\\lwate\\Documents\\coding_projects\\securekeygen'],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='SecureKeyGen',
    description='A secure key generator for cryptographic operations, generating keys locally without logging any interactions.',
    icon='key_pixel_art_32x32.ico',  # Ensure this path is correct
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)

coll = COLLECT(
    exe,
    a.binaries,
    a.zipfiles,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='SecureKeyGen',
)